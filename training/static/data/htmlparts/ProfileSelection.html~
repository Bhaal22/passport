<div id="profilesDisplay">
  <form  action="#passport" method="get" class="container">
    {% csrf_token %}
    <select ng-model="profile" ng-options="p.name for p in profiles">
      <option value="0">{{ p.resource_uri }} </option>
    </select>
    <input type="submit" />
  </form>
</div>

<!-- {{profileuri}}/{{item.id}} -->

<!--
<div id="actionsWrap">
    <div data-ng-repeat="item in profiles" id="{{item.id}}" class="container">
        <a href="#passport">
            <div class="icon"></div>
            <h2>{{item.name}}</h2>
            <div class="description">{{item.description}}</div>

        </a>
    </div>
</div>
-->
